# Configuration

This directory will contain the configuration files defined by the architecture
specification:

- `environment.yaml`
- `jepa.yaml`
- `dynamics.yaml`
- `controller.yaml`

Concrete values should be added after the initial decision gates—especially the
RIS group count, high-level update interval, observable mobility variables, and
channel-correlation model—have been resolved.
